name: Actions PR CI
run-name: Actions PR CI

permissions:
  contents: read

on:
  pull_request:
    types:
      - opened
      - reopened
      - synchronize

jobs:
  noop:
    name: PR CI
    runs-on: ubuntu-latest
    steps:
      - name: nothing to lint
        run: echo "This repo contains only reusable workflows; no Terraform code or pre-commit config to lint on PR."
